import {
  RuleSet
} from '../src';

describe('规则引擎', () => {

  test('支持常用的数值、字符串、布尔、null', () => {
    // const rule = new RuleSet(["rule 'vouchersave' { \
    //   when{\
    //     c: Event c.name == 'TestOrder.saving'; \
    //     v: TestOrderData;\
    //   }\
    //   then{\
    //     v.detail[0].num = 100;\
    //   }\
    // }"], {}, {})
    //
    // rule.execute([]);
  })

})
